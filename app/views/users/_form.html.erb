<%= form_with(model: user, local: true, class: 'form-group') do |form| %>
    <% if user.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

          <ul>
            <% user.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>



    <div class="signup-field">

      <%= form.text_field :email, class: 'form-control', placeholder: '이메일 주소' %>
    </div>

    <div class="signup-field">

      <%= form.text_field :name, class: 'form-control', placeholder: '사용자 이름' %>
    </div>

    <% if action_name == 'edit' %>
    <div class="signup-field">
      <%= form.file_field :profile_img, class: 'form-control', placeholder: '프로필 사진' %>
    </div>
    <%end%>

    <div class="signup-field">

      <%= form.password_field :password, class: 'form-control', placeholder: '비밀 번호' %>
    </div>

    <div class="actions">
      <%= form.submit  class: 'btn btn-primary signup-submit'%>

    </div>

<% end %>